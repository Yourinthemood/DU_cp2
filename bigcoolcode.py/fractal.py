#Du Larose p1 fractal triangle thing

#import turtle

    # Change the call name for turtle to a simplier letter
    # Make screen a certain integer to make the fractal easier to divide in order to create the visual

# ask user for how many times the fractal should be multiplied or a depth of ractal (1-5)
      # (for example if the user inputs 5, the fractal will be multiplied by 50 making it very deatailed)

#define the call function to start the program
'''start_main():''' 
    #instance that start it will be gone through a conditional that checks if the user has selected a backround color and a pen color thickenss, and such
    # If it goes through then start the program

    #===============
    # Name variables
    #===============

    #depth = ???

    #
    